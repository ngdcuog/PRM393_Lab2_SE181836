/// Represents a journal with publication count statistics.
class JournalStat {
  const JournalStat({required this.id, required this.name, required this.count});

  final String id;
  final String name;
  final int count;
}
